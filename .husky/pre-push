#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔨 Push 전 빌드 테스트 및 빌드 확인을 실행합니다..."

# 빌드 체크
npm run build

# 빌드가 실패하면 push 중단
if [ $? -ne 0 ]; then
  echo "❌ 빌드 실패! Push가 취소되었습니다."
  exit 1
fi

echo "✅ 빌드 성공! Push를 진행합니다."